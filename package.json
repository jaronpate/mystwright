{
    "name": "mystwright",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "run-tui": "bun --cwd=apps/tui run --env-file=../../.env index.ts",
        "run-api": "bun --cwd=apps/api run --env-file=../../.env start",
        "run-web-ui": "bun --cwd=apps/frontend run --env-file=../../.env dev"
    },
    "workspaces": [
        "libs/*",
        "apps/*"
    ]
}
