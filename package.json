{
    "name": "mystwright",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "run-api": "bun --cwd=apps/api run --env-file=../../.env start",
        "run-api:watch": "bun --cwd=apps/api run --env-file=../../.env watch",
        "run-web": "bun --cwd=apps/web run --env-file=../../.env dev",
        "build-web": "bun --cwd=apps/web run --env-file=../../.env build"
    },
    "workspaces": [
        "libs/*",
        "apps/*"
    ]
}
